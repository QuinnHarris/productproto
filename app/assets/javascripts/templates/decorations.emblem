.decorations
  = option-list items=techniques class="techniques" selectedId=techniqueId selected=technique
  .technique
    = option-list items=currentLocations class="locations"

    .location
      if useColors
        = render 'decorationColors'
      if useNumber
        = render 'decorationNumber'